services:
  pipeline:
    build:
      context: .
      dockerfile: Containerfile
    environment:
      - PIPELINE_MODE=${PIPELINE_MODE:-test}
    volumes:
      - ./data:/app/data
      - ./scripts:/app/scripts
